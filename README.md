# BDA Backend Containerized version

> Pour d'autre OS que Linux

Assurer vous que Docker est installer (avec docker compose) et lance la commande

```cmd
docker compose up 
```

> Note : Pour supprimer `docker compose down`