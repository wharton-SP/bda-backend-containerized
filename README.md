# BDA Backend Containerized version
> For other operating systems than Linux

Make sure Docker is installed (with docker compose) and run the command

```cmd
docker compose up 
```

> Note: To remove `docker compose down`
